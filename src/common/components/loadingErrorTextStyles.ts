import { StyleSheet } from 'react-native'
import { scale } from 'react-native-size-matters'

export const loadingErrorTextStyles = StyleSheet.create({
    errorText: {
        fontSize: scale(12),
        marginBottom: scale(10)
    }
})